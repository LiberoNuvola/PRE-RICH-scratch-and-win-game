# PoC-0 — Materios CanonicalCheckpoint

**Not B3.** Untrusted adapter extracts a reproducible checkpoint from a real Materios node.

## Run (local node)

```bash
cd poc/materios-checkpoint
npm install
MATERIOS_RPC=http://127.0.0.1:9945 npm run poc0